如果客户端类型是机密的，则客户端和授权服务器建立适合于授权服务器安全需求的客户端身份验证方法。授权服务器可以接受满足其安全需求的任何形式的客户机身份验证。

机密客户机通常颁发\(或建立\)一组客户机凭据，用于使用授权服务器进行身份验证\(例如，密码、公共/私有密钥对\)。

授权服务器可以使用公共客户端建立客户端身份验证方法。但是，授权服务器不能依靠公共客户端身份验证来标识客户机。

客户端不得在每个请求中使用多种身份验证方法。

